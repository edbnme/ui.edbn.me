/**
 * =============================================================================
 * TEST UTILITIES INDEX
 * =============================================================================
 *
 * Central export point for all test utilities.
 *
 * @module test
 * @example
 * ```tsx
 * import { render, screen, pressEscape, waitFor } from "@/test";
 * ```
 * =============================================================================
 */

export * from "./utils/test-utils";
